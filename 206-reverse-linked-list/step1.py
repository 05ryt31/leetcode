# 動かないコード

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        if not None:
            return None

        reverse = []
        reverse.add(this.val)

        while this.next:
            reverse.append(this.next)

        return reverse